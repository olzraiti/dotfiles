#!/bin/bash

direction="bottom"
namespace=""
current=$(bspc query -D -d)

while getopts 'd:n:g' flag; do
	case "${flag}" in
		d) 
			direction="$OPTARG"
		;;
		g)
			current=""
		;;
		n)
			namespace="$OPTARG"
		;;
  esac
done
shift $((OPTIND-1))

echo $current"_"$direction"_pull_window"$namespace
